# ── Role: HOST (MCU) ────────────────────────────────────────────────
CONFIG_ESP_HOSTED=y

# ── Features: BT + the Bluedroid hosted-HCI port ────────────────────
CONFIG_ESP_HOSTED_HOST_FEAT_BT=y
CONFIG_ESP_HOSTED_HOST_BT_PORT_BLUEDROID=y
# P1: explicit eh_host_bluedroid_init() (auto-init path is a follow-up).
CONFIG_ESP_HOSTED_HOST_BT_PORT_BLUEDROID_AUTO_INIT=n

# ── Transport: MCU-side default ─────────────────────────────────────

CONFIG_FREERTOS_HZ=1000

#
# BT host stack (Bluedroid) — provided by IDF; local controller disabled
# because the actual BT controller runs on the ESP-Hosted co-processor.
#
CONFIG_BT_ENABLED=y
CONFIG_BT_CONTROLLER_DISABLED=y
CONFIG_BT_BLUEDROID_ENABLED=y
CONFIG_BT_BLE_42_FEATURES_SUPPORTED=y
CONFIG_BT_BLE_50_FEATURES_SUPPORTED=n

#
# Wi-Fi Remote (typically required alongside Hosted)
#

# Role: HOST (MCU)
CONFIG_ESP_HOSTED=y

# Features: BT. The stack is chosen by the IDF BT Kconfig (BT_BLUEDROID_ENABLED
# below); the app calls esp_hosted_bt_host_stack_setup().
CONFIG_ESP_HOSTED_HOST_FEAT_BT=y

# Transport: MCU default

CONFIG_FREERTOS_HZ=1000

# Bluedroid (BLE side), local controller disabled (CP supplies it).
CONFIG_BT_ENABLED=y
CONFIG_BT_CONTROLLER_DISABLED=y
CONFIG_BT_BLUEDROID_ENABLED=y
CONFIG_BT_BLE_42_FEATURES_SUPPORTED=y
CONFIG_BT_BLE_50_FEATURES_SUPPORTED=n

# Wi-Fi Remote (typically required alongside Hosted)

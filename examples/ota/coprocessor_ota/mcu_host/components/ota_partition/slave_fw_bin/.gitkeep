# This directory should contain .bin firmware files for Partition OTA
# Place your slave firmware binaries here before building
# Git tracks this file to ensure the directory exists in the repository

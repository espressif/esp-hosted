config ESP_HOSTED_HOST_VSERIAL_LINUX
	bool
	default y if ESP_HOSTED_HOST_TYPE_POSIX
	default n
	depends on ESP_HOSTED_HOST

config ESP_HOSTED_HOST_VSERIAL_LINUX_READY
	bool
	default y if ESP_HOSTED_HOST_VSERIAL_LINUX
